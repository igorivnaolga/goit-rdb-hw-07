1. SELECT 
	id,
	date, 
	YEAR(date) as year,
	MONTH(date) as month, 
	DAY(date) as day
FROM
	orders;

2. SELECT 
	id,
	date, 
	DATE_ADD(date, INTERVAL 1 DAY) AS date_plus_one_day
FROM
	orders;

3. SELECT 
	id,
	date, 
	UNIX_TIMESTAMP(date) AS unix_timestamp
FROM
	orders;